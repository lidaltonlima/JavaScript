let values = [8, 1, 7, 4, 2, 9]

// for (let pos = 0; pos < values.length; pos++) {
//     console.log(`Position: ${pos}; Value: ${values[pos]}`)
// }

for (pos in values) {
    console.log(`Positon: ${pos}; Values: ${values[pos]}`)
}